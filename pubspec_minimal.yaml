name: dart_acdc
description: A zero-config, opinionated HTTP client
version: 0.2.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  crypto: ^3.0.7
  dio: ^5.4.0
  dio_cache_interceptor: ^4.0.5
  jwt_decoder: ^2.0.1
  meta: ^1.17.0

dev_dependencies:
  http_mock_adapter: ^0.6.1
  mockito: ^5.4.4
  test: ^1.26.3
